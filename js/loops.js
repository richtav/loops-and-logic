// different ways to incrememnt variables
//1. x = x + 1;
//2. x+= 1;
//3. x++;

for (var x = 1; x < 10; x++) {
  console.log(x);
}


var y = 7;
while (y > 0) {
  console.log(y);
// the conditional must at some point stop to there must be something that happens to the variable each time
y--;
}
